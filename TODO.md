# TODO

待辦事項清單。開始進行某項工作時，將它移到 `PROGRESS.md`。

## 格式

```
- [ ] 項目描述（優先序：高／中／低）
```

## 待辦

- [ ] 考慮把 AM 的 API key 改存系統鑰匙圈（macOS 鑰匙圈／Windows 認證管理員）（優先序：低）
- [ ] 使用者驗收 v0.1.0，通過後將 `develop` 合併到 `master` 並打 tag `v0.1.0`（優先序：高）
- [ ] 在 Windows 實機測試：安裝腳本、終端選單（方向鍵、中文輸入與顯示）、`am web`、開機自動執行（優先序：高）
- [ ] 實測 `am web` 自動開啟瀏覽器、網站表單的實際操作（優先序：中）
- [ ] 發佈到 GitHub Releases（需要 push，由使用者決定時機）（優先序：中）
- [ ] 提供 Homebrew 安裝方式（需要另建 tap 儲存庫）（優先序：低）
- [ ] 執行檔程式碼簽章（macOS 公證、Windows 簽章），避免系統跳出安全警告（優先序：低）
- [ ] 改善他人首次安裝體驗：安裝腳本最後詢問或自動完成 PATH（修改 `~/.zshrc`）、開機自動執行、開啟管理網站；`am` 啟動前先檢查 Claude Code 是否安裝；沒有 API 服務商時的引導（優先序：中，使用者暫緩決定）
- [ ] 一行指令安裝（`curl ... | sh`），需先發佈到 GitHub Releases（優先序：中，使用者暫緩決定）
- [ ] 解除安裝方式（執行檔、開機自動執行、設定檔）（優先序：低）
