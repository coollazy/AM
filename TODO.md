# TODO

待辦事項清單。開始進行某項工作時，將它移到 `PROGRESS.md`。

## 格式

```
- [ ] 項目描述（優先序：高／中／低）
```

## 待辦

- [ ] 決定 AM 網站的技術棧與架構（優先序：高）
- [ ] AM 網站：管理 `~/.ai-profiles/` 設定檔（新增、編輯、刪除）（優先序：高）
- [ ] AM 網站：驗證設定檔的模型名稱在該服務商是否可用（優先序：高）
- [ ] 確認 MixRoute 正確的 API 網址，`api.mixroute.ai` 與 `console.mixroute.io` 目前各有一個設定檔在用（優先序：中）
- [ ] 訂閱制設定檔改成清除所有 `ANTHROPIC_*` 變數，目前只清除其他設定檔有用到的 5 個（優先序：低）
- [ ] `ai` 選單改成只讀取設定檔名稱，不執行整個設定檔，避免變數殘留到其他方案（優先序：低）
- [ ] 考慮把 API key 搬進 macOS 鑰匙圈（優先序：低）
