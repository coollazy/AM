# TODO

待辦事項清單。開始進行某項工作時，將它移到 `PROGRESS.md`。

## 格式

```
- [ ] 項目描述（優先序：高／中／低）
```

## 待辦

- [ ] AM 網站：管理服務商設定（新增、編輯、刪除），存於 `~/.am/`（優先序：高）
- [ ] `am` 指令（取代現有 `ai`）：第一層選服務商，第二層即時查詢並列出該服務商可用模型；訂閱制沒有第二層（優先序：高）
- [ ] AM 網站開機自動在背景執行（優先序：中）
- [ ] 提供給其他人安裝的發佈方式（優先序：中）
- [ ] 非 Claude 模型預設套用輸出上限 8192，網站可為模型個別設定輸出上限與上下文大小（優先序：中）
- [ ] 考慮把 AM 的 API key 改存系統鑰匙圈（macOS 鑰匙圈／Windows 認證管理員）（優先序：低）
